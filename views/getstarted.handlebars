<main class="flex-fill">
    <div class="container my-5">
        <!-- Instructions -->
        <div class="text-center mb-4">
            <h2>Search for Pets</h2>
            <p>Please enter your search parameters in the following order, separated by spaces:</p>
            <p><strong>Distance from location, Type of pet, Breed, Country, State, City</strong></p>
        </div>

        <!-- Search Form -->
        <div class="d-flex justify-content-center">
            <form class="d-flex" id="anotherSearchForm">
                <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search"
                    id="searchInput">
                <button class="btn btn-outline-success" type="submit">Search</button>
            </form>
        </div>
    </div>
</main>
